# MongoDB Atlas connection URI (e.g. mongodb+srv://username:password@cluster.xxxx.mongodb.net/dbname?retryWrites=true&w=majority)
MONGODB_URI=

# Gemini API Key (managed by the platform, but can be customized here if needed)
GEMINI_API_KEY=
